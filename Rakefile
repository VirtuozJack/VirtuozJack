# encoding: UTF-8
require "rubygems"
require "tmpdir"
require "bundler/setup"
require "jekyll"
require "bourbon"

# Change your GitHub reponame
GITHUB_REPONAME = "VirtuozJack/VirtuozJack"
GITHUB_REPO_BRANCH = "gh-pages"